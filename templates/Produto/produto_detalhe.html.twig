{% extends 'base.html.twig' %}

{% block title %}Manter produto{% endblock %}

{% block body %}
<div class="container">    
    {{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    {{ form_end(form) }}
</div>    
{% endblock %}